"use client ";
import { Modal}  from "@/components/ui/modal";
export const useStoreModal =()=>{
    const storeModal =useStoreModal();
    return(
   <Modal   title="Create Store"
   description="Add a new store to manage products and categeories"
   isOpen={false}
   onClose={() =>{}}
  > Future Create Store Form
   
   </Modal>
    );
};